#include<iostream>
#include<set>

using namespace std;

int main()
{
    set<int>s;
    s.insert(3);
    s.insert(6);
    s.insert(2);
    s.insert(2);
    s.insert(10);
    cout << s.count(2) << endl; // To check wheather an element is present to that SET or not
    return 0;
}
